apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: zihao-csi
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: csi-hostpath-sc # defined in csi-storageclass.yaml